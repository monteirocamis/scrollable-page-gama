import { AnimationClip, AnimationMixer, Object3D } from 'three';
/**
 * AnimationHelper is a helper class used by AnimationHelperPlugin that allows the extraction of all the animation clips and bind it with AnimationMixer
 * For internal use only at the moment.
 */
export declare class CustomAnimationHelper {
    /**
     * Animation mixer for a 3D model
     */
    mixer: AnimationMixer;
    private _model;
    private _animations;
    private _playingAnimations;
    private _clock;
    /**
     * Loads the model in the helper
     * @param model - 3D model to use
     */
    loadModel(model: Object3D): void;
    /**
     * Returns mixer of the model
     */
    get getMixer(): AnimationMixer;
    /**
     * Returns animation clips of the model
     */
    get getAnimationClips(): AnimationClip[];
    /**
     * Plays all the animation clips that are present in the 3D model
     * @param loop - default is 'false', if the clip needs to be looped continously then set it to 'true'
     */
    playAllAnimations(loop: boolean): Promise<void>;
    /**
     * Reverses all the animation clips that are present in the 3D model
     * @param loop - default is 'false', if the clip needs to be looped continously then set it to 'true'
     */
    reverseAllAnimation(loop: boolean): Promise<void>;
    /**
     * Plays the single clip from the Helper of a 3D model
     * @param clip - Animation Clip of the 3D model that needs to be played
     * @param loop - default is 'false', if the clip needs to be looped continously then set it to 'true'
     * @param timescale - timescale of the animation clip that will played. Default is 1
     * @param reverse - to reverse the clip set it to true. Default is false
     * @param resetTime - resets the mixer time. Default is false
     */
    playClip(clip: AnimationClip, loop: boolean, timescale?: number, reverse?: boolean, resetTime?: boolean): Promise<void>;
    /**
     * Plays the clip by its name from the Helper of a 3D model
     * @param name - name of the animation clip that is present in 3D model
     * @param loop - default is 'false', if the clip needs to be looped continously then set it to 'true'
     * @param timescale - timescale of the animation clip that will played. Default is 1
     * @param reverse - to reverse the clip set it to true. Default is false
     */
    playClipByName(name: string, loop: false, timescale?: number, reverse?: boolean): void;
    /**
     * Update loop to update the mixer
     */
    update(): boolean;
}
//# sourceMappingURL=CustomAnimationHelper.d.ts.map